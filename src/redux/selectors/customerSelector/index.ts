export const listCustomerSelector = (state: any) => state.customerList;

export const customerInfomationSelector = (state: any) =>
    state.customerInfomation;

export const editCustomerSelector = (state: any) => {
    state.editCustomer;
};
